// This is where the widget that the [HomeScreen] uses and might be used
// in another place. Therefore they must be exposed
// If the widgets are intended to be used in many places, then they are
//should be placed under `common_widgets`

import 'package:flutter/material.dart';

class SomeWidget extends StatelessWidget {
  const SomeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
